# {{PROJECT_NAME}}

<!-- 이 저장소가 무엇인지 한두 줄. 구조·스택은 README.md가 정본이고, 이 문서는 **여기서
     일하는 방식**만 다룬다. -->

운영 루프의 로직은 이 저장소에 없다 — `superokok/gh-workflows`의 재사용 워크플로우에 있고,
`.github/workflows/`의 파일들은 트리거와 이 프로젝트 고유 값만 갖는다.

**작업 루프 자체(브랜치·PR·WIP 제한·릴리스·알림 철학·위험 경로 기준)도 이 저장소가 소유하지
않는다.** 정본은 `superokok/project-template`의 `common/`이고 `template-sync.yml`이 동기화한다.
아래 한 줄이 그 지침을 가져온다 — **여기서 고치면 다음 동기화 때 덮이므로** 고칠 것이 있으면
정본 저장소에 PR을 낸다.

@.claude/common/work-loop.md

---

## ✅ 자동으로 진행해도 되는 것

- 코드 읽기·탐색·검색, 테스트 실행, 타입체크
- 작업 브랜치에서의 커밋·push, `gh pr create`
- 이슈·PR 코멘트 읽기, `gh` 조회 명령

## 🚫 항상 먼저 확인해야 하는 것

<!-- 이 저장소에서 사람 확인이 필요한 것. 훅이 실제로 막는 것(.env·파괴적 git·gh api 쓰기)은
     공통 지침에 있으니, 여기엔 **이 프로젝트 고유의 것**을 적는다. -->
- <!-- 예: 운영 DB 관련 작업 / 데이터 삭제 / 의존성 제거 -->
- 저장소 설정 변경(브랜치 보호, 시크릿, 라벨 규칙)

## 🗄️ 사전 계획 게이트

`{{PLAN_ONLY_PATHS}}`를 건드리는 작업은 **구현 전에 짧은 계획을 먼저 제시하고 승인받는다.**
마이그레이션은 데이터 손실을 diff로 못 잡기 때문이고, 적용된 마이그레이션은 절대 수정하지 않는다.

## 🧪 검증 — 로컬에서 돌리는 것

```bash
{{VERIFY}}
```

CI가 도는 것과 같다. **이 게이트가 덮는 범위를 과신하지 않는다** — 자세한 근거는 공통 지침.

## 🔒 위험 경로

판정 정본은 `.github/workflows/enable-auto-merge.yml`의 `risk-paths` 정규식이다. 기준(기계가
원리적으로 못 잡는 곳에만 사람을 부른다)은 공통 지침에 있고, 여기엔 **이 저장소가 기준을
적용하며 갈린 지점**만 적는다.

## 🏗️ 알아둘 것

- **필수 체크는 `{{REQUIRED_CHECKS}}`.**
- **기본 브랜치는 `{{PRODUCTION_BRANCH}}`이고 통합 브랜치는 `{{INTEGRATION_BRANCH}}`다.**
  브랜치에 매이지 않는 이벤트로 깨어나는 호출부(`claude-agent`·`claude-fix`·`claude-review`의
  `workflow_run`·`template-sync`)는 **기본 브랜치에 있는 파일이 동작한다** — 통합 브랜치에만
  머지하면 효력이 없고 릴리스까지 가야 한다.
- **Actions 분은 job 단위로 올림 과금된다** — 짧은 단계를 별도 job으로 나누지 않는다.
